#include <stdio.h>

/**
 * main - Entry point - prints the alphabet in lowercase
 *
 * Results: Always 0 (Success)
*/

int main(void)
{
	/* local variable */
	int n = 'a';

	/* while loop */
	while (n <= 'z')
	{;
	putchar(n);
	n++;
	}
	return (0);
	putchar('\n');
}
